# Open Move

링크로 접속하면 같은 공개 서버에서 다른 사람들과 이동만 할 수 있는 최소 멀티플레이 프로토타입입니다.

## 로컬 실행

```bash
npm install
npm start
```

브라우저에서 `http://localhost:3000` 접속.

## 조작
- PC: WASD / 방향키
- 모바일/태블릿: 왼쪽 아래 조이스틱

## 공개 서버로 만들기
Node.js 서버를 지원하는 호스팅(Render, Railway 등)에 이 폴더를 올리고 시작 명령을 `npm start`로 설정하면 됩니다.
